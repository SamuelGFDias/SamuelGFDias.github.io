# Configurações de Build Flutter

# Usar Firestore como fonte de dados (true/false)
# Quando false, usa assets locais (assets/contents/home.json)
USE_FIRESTORE=      

# Usar Cloud Functions para leitura/escrita (true/false)
# Requer plano Firebase Blaze e deploy das Functions
# Quando false, acessa Firestore diretamente do cliente
USE_FUNCTIONS=false

# =============================================
# Como usar:
# =============================================
# 
# 1. Modo Local (desenvolvimento):
#    flutter run -d chrome
#    (usa assets/contents/home.json)
#
# 2. Modo Firestore (sem Functions):
#    flutter run -d chrome --dart-define=USE_FIRESTORE=true
#    (acessa Firestore diretamente, regras de segurança aplicam)
#
# 3. Modo Firestore + Functions (produção):
#    flutter run -d chrome --dart-define=USE_FIRESTORE=true --dart-define=USE_FUNCTIONS=true
#    (usa Cloud Functions para validação adicional)
#
# =============================================
# Build para produção:
# =============================================
#
# flutter build web --dart-define=USE_FIRESTORE=true --dart-define=USE_FUNCTIONS=true
#
